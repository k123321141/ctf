程式裡面疑似有bin/bash的字串
main宣告了一個local variabl 0x80的字串
並且用memset設為0

然後close(1) 關閉stdout

main+56     memset(arr, 0, 0x80)    
main+71     printf('Input :')
main+81     close(1)
main+106    read(0, arr, 0x80 )
main+126    printf(arr)
main+136    exit(0)


這是一個format string的題目
可是需要思考 既然關閉了stdout要怎麼做

